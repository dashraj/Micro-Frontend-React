import { FC, memo } from 'react';
import { HostEventBusLogger } from './EventBusLogger';
export const EventBusAdaptor = () => {
    return (
        <>
            <HostEventBusLogger />
        </>
    );
};